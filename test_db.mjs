import { createClient } from '@supabase/supabase-js'; const supabaseUrl = process.env.NEXT_PUBLIC_SUPABASE_URL; const supabaseKey = process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY; const supabase = createClient(supabaseUrl, supabaseKey); supabase.from('projects').select('wbs, name').then(({data}) => { console.log('Total:', data.length); const configs = data.filter(p => p.wbs && (p.wbs.includes('IMPORTANT') || p.wbs.includes('SAFETY') || p.wbs.includes('Tracker') || p.wbs.includes('Dashboard'))); console.log('Config rows:', configs); });
